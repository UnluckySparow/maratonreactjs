export const Get_contacts = () =>{
    JSON.parse(localStorage.getItem("contacts"));
    return (dispatch) =>{
        dispatch({
            type:"Get-contacts",
            payload:contacts
        });
    }
}
